# AGENTS.md: Regional Tourism Scan 개발 지침 및 스펙 정의서

## 0. 프로젝트 방향 (2026-08 개정)

**이 프로젝트의 성과는 "웹사이트를 만든 것"이 아니라 "전국 시군구 관광구조를 실제로 진단해, 기존 관광통계와 다른 정책 결론을 도출한 것"이다.**

UI/디자인은 이미 충분한 완성도를 갖췄으므로, 지금부터는 새로운 화면·기능을 늘리지 않는다. 다음 4단계 진단 로직을 최우선으로 완성한다.

```
유사지역 비교 → 취약영역 탐지 → 원인분해 → 정책 우선순위
```

핵심 재정의: 이 서비스는 **"관광지역 점수를 매기는 서비스"가 아니라 "지역 관광현황을 데이터로 진단해 어떤 관광정책을 먼저 시행해야 하는지 알려주는 서비스"**다. 점수(R-GAP, TCEI)는 결과가 아니라 진단 과정의 중간 산출물이며, 화면 전면에 내세우지 않는다.

---

## 1. 프로젝트 기본 정보
* **프로젝트명**: Regional Tourism Scan (엔진명: R-GAP, Regional Recoverable Tourism Value Gap Engine)
* **서비스 정의**: "관광객이 얼마나 많은가"가 아니라 "해당 지역은 무엇이 부족하며, 어떤 관광정책을 먼저 시행해야 하는가"를 진단하는 데이터 기반 정책 의사결정 서비스
* **핵심 목적**: 한국관광 데이터랩 및 외부 공공데이터를 활용해 지자체별 관광구조를 유사지역(Peer Group)과 비교하고, 취약영역의 원인을 분해하여 정책 우선순위 TOP 3와 근거를 제공한다.
* **공모전 핵심 결과물** (웹사이트 자체가 아니라 아래 분석 산출물이 성과임):
  1. 전국 시군구 관광구조 전수진단
  2. 방문객은 많지만 체류/숙박/소비 성과가 낮은 지역 탐색
  3. 서로 다른 원인을 가진 대표지역 3개 선정 (예: 숙박공급 제약형 / 야간콘텐츠 부족형 / 관광지 집중형)
  4. 기존 방식(방문객 증가율 중심)과 정책 결론이 달라지는 사례 발굴 — "정책판단의 변화"를 대표 성과로 제시

---

## 2. 서비스 아키텍처 및 기술 스택
* **Frontend**: React (Vite) — `src/live.tsx`가 현재 메인 엔트리
* **Backend**: Python FastAPI (`backend/app`) — KTO 데이터랩 연동 및 산출 로직
* **Database**: PostgreSQL / PostGIS (지자체 경계 공간 데이터 및 시계열 지표 저장, 고도화 단계)
* **Visualization**: Recharts류 경량 차트, Leaflet (전국 지자체 지도)

---

## 3. 핵심 데이터 스펙 및 파이프라인

### 3.1. 연동 데이터 소스
* **한국관광 데이터랩**: 통신 기반 외지인 방문자 수, 순 방문자, 숙박 방문자 비율, 평균 체류시간, 평균 숙박일, 신용카드 관광소비액(업종별, 내/외지인), 내비게이션 검색 및 이동, SNS 언급량/키워드, 야간관광 지표, 관광지 집중률
* **지방재정365**: 지자체별 재정 공시 및 세출 데이터 (고도화 단계 연계)

### 3.2. 데이터 전처리 및 주의사항
* **원시 비율(Raw Ratio) 사용 금지**: 신용카드 소비액을 방문자 수로 단순 나눈 '1인당 소비액' 산출 금지. 동일 월 및 유사 관광구조 집단 내에서 표준화 후 잔차(Residual) 분석 적용.
* **데이터 한계 표기 컴포넌트 필수**: 내비게이션 데이터의 차량 이동 편향, SNS 언급량의 추세 활용 한계 등을 UI 내 툴팁·경고 메타정보로 상시 노출.

### 3.3. 데이터 신뢰도 표시 (전면 유지)
모든 수치는 아래 4단계 중 하나로 명확히 라벨링한다. 데이터가 없는 결과를 확정값처럼 표현하지 않는다.
* **실측 데이터**: KTO API에서 직접 수신한 원값
* **파생지표**: 실측값을 조합·정규화한 값 (예: 강도 지수, 백분위)
* **모델 추정값**: 회귀·분해 등 통계 모델을 거친 값 (예: TCEI, R-GAP, 원인분해 비중)
* **향후 분석 가능 지표**: 아직 데이터가 적재되지 않아 산출 불가한 항목 (예: 관광지 집중도, 계절성)

### 3.4. LIVE 표현 가이드
데이터가 초/분 단위 실시간이 아니므로 "실시간 방문자" 같은 표현을 쓰지 않는다. 대신: "최신 방문자 데이터", "최근 관광현황", "일별 방문자 현황", "Data Lab 최신 데이터" 등을 사용한다.

---

## 4. 진단 로직 (핵심)

### 4.1. 진단 6축
전국 순위·백분위 나열 대신, 아래 6축을 중심으로 지역을 진단한다.
1. 관광수요
2. 체류
3. 숙박
4. 관광소비
5. 공간확산
6. 계절/시간 안정성

각 축은 **유사 관광지역(Peer Group)** 대비 상대값(%p 또는 배수)으로 표시한다. 전국 평균 대비 비교는 보조 지표로만 사용한다.

예시 표시:
```
관광수요   유사지역 대비 +18%
체류       유사지역 대비 -17%
숙박       유사지역 대비 -29%
관광소비   유사지역 대비 -4%
공간확산   유사지역 대비 -22%
```

숫자 뒤에는 반드시 자연어 진단 문장이 따라야 한다.
> "제주시는 관광객 유입과 체류에는 문제가 없지만, 확보된 관광수요가 소비와 지역 내 확산으로 충분히 연결되지 않고 있습니다."

"점수"가 아니라 "정책적으로 무엇이 문제인지"가 화면의 결론이어야 한다.

### 4.2. 진단 파이프라인
```
데이터
→ 유사지역 비교 (Peer Group Comparison)
→ 취약영역 탐지 (Weakness Detection)
→ 원인분해 (Root-Cause Decomposition)
→ 정책 우선순위 (Policy Priority)
```
기존의 "데이터 → 점수/R-GAP → 예산" 구조는 폐기한다. 점수는 취약영역 탐지 단계의 중간 산출물로만 쓰인다.

### 4.3. 원인분해 원칙 — 같은 증상, 다른 원인, 다른 처방
지표가 낮다고 곧바로 인프라 확충을 제안하지 않는다. 반드시 2차 지표로 원인을 분해한 뒤 정책을 제안한다.

**예시: 숙박 지표가 낮은 경우**

| 조건 | 유형 | 정책 방향 |
|---|---|---|
| 숙박률 낮음 + 객실공급 부족 | 숙박공급 제약형 | 숙박 인프라 확충 / 빈집 활용 / 민간투자 유치 |
| 숙박률 낮음 + 객실공급 충분 + 야간관광·체류시간 낮음 | 숙박수요 연결 부족형 | 야간콘텐츠 개발 / 교통 연계 / 숙박상품 결합 |

이 원인분해 로직을 다른 축(체류, 소비, 공간확산)에도 동일한 원칙으로 확장한다: 1차 증상 지표 + 2차 구조 지표(공급/접근성/콘텐츠 등)를 함께 봐야 유형이 결정된다.

### 4.4. 지역 관광유형 판별 (자동 분류 뱃지)
* 수요부족형 / 체류누수형 / 숙박공급제약형 / 숙박수요연결부족형 / 소비누수형 / 단일거점편중형(관광지 집중형) / 야간이탈형(야간콘텐츠 부족형) / 계절의존형

### 4.5. 정책 우선순위 TOP 3 (최종 Output)
진단 파이프라인의 최종 산출물. 각 지역마다 아래 형식으로 제공한다.

```
[○○시 정책 우선순위]

1순위. 체류·숙박 강화
근거: 관광수요 +18% / 체류 -17% / 숙박 -29%

2순위. 관광지 간 연결
근거: 특정 관광지 집중도 높음 / 연관관광지 이동 낮음

3순위. 야간관광 기반 강화

[우선순위 낮음] 추가 관광홍보 — 방문수요 자체는 이미 충분하기 때문
```

우선순위가 낮은 항목(대개 "홍보 확대")과 그 이유를 함께 제시해, "방문객이 이미 충분한 지역에 홍보 예산을 더 쓰는" 기존 관행과의 차이를 드러낸다.

### 4.6. TCEI / R-GAP 산출식 (모델 추정값 — 화면 전면에 노출하지 않음)
점수는 진단 로직의 내부 계산에만 사용하고, UI에서는 점수보다 진단 문장을 먼저 보여준다.

동일 월 및 유사 관광구조 집단 내 상대 위치(0~1)를 기준으로 기하평균 산출:
* **체류전환 (S)**: `sqrt(P(평균체류시간) × P(숙박방문자비율))`
* **소비전환 (C)**: 관광수요지수 대비 실제 신용카드 관광소비의 회귀 잔차(Residual) 상대위치
* **공간전환 (D)**: `1 - P(관광지 집중도)`
* **시간전환/안정성 (B)**: 월별 계절변동계수 역산 값
* **TCEI 종합식**: `TCEI = 100 × (S × C × D × B)^(1/4)`

**프론티어 기준선**: 구조적 조건(관광유형, 숙박공급, 접근성, 수요 등)에 따른 75분위 회귀 모델
`TCEI_frontier_i = Q0.75(TCEI | X_i)`

**R-GAP**: `R-GAP_i = max(0, TCEI_frontier_i − TCEI_actual_i)`

요소별 누수 기여도(체류·숙박, 소비, 공간분산, 야간·계절)를 원인분해(4.3)의 입력으로 사용한다.

---

## 5. 프론트엔드 화면 구성 (Flow 순서 고정)

```
전국지도
→ 지역 선택
→ 관광현황 진단
→ 유사지역 비교
→ 취약영역 탐지
→ 원인진단
→ 지역 관광유형 판별
→ 정책 우선순위 TOP 3
→ (선택) 예산 시뮬레이션
→ (향후) 정책 시행 후 효과검증
```

### 5.1. [View 1] 전국 지도 (Main Dashboard)
지자체 클릭 시 관광유형 뱃지(4.4)를 표시한다. 전국 순위 나열이 아니라 유형 분포를 보여주는 데 초점을 둔다.

### 5.2. [View 2] 지역 상세진단 (핵심 화면)
여러 지표를 한 번에 나열하지 않고 4.1의 6축 중심으로 재구성한다. 각 축은 유사지역 대비 상대값 + 자연어 진단 문장 순서로 배치한다. 종합점수(R-GAP/TCEI)는 이 화면의 주인공이 아니다 — 표시하더라도 진단 문장 아래, 보조 정보로만 위치시킨다.

### 5.3. [View 3] 원인진단 & 정책 우선순위 (최종 Output)
4.3(원인분해) → 4.4(유형 판별) → 4.5(TOP 3) 순서로 표시. 이 화면이 서비스의 핵심 결과물이다.

### 5.4. [View 4] 예산 시뮬레이터 (보조 기능으로 위치 변경) — **미구현: 화면에서 제거된 상태**
**변경 전**: 사용자가 처음부터 체류 40%, 소비 25%, 공간 20% 등 임의 비율을 설정 — 가중치 근거를 설명하기 어려움.

**변경 후**: 정책 우선순위(4.5) 결과를 먼저 제시한 뒤, 그 근거 위에서 "10억 원의 추가 예산이 있다면 어떻게 배분할 것인가"를 탐색하는 보조 도구로 배치한다. 시뮬레이터는 모델의 근거가 아니라 진단 결과를 탐색하는 what-if 도구다. 초기 슬라이더 값은 진단에서 나온 우선순위 비중을 기본값으로 제안하되, 사용자가 조정할 수 있게 한다.

**현재 상태**: `POST /v1/budget/portfolio`(allocate_budget)는 백엔드에 남아 있지만, 프론트엔드 화면에서는 완전히 제거되어 있다. 원인은 우선순위(5.3) 로직이 Peer Group 중심 진단으로 여러 차례 재구성되며 시뮬레이터 연동이 함께 갱신되지 않았기 때문이다. 재도입하려면 5.3의 현재 우선순위 카드(취약 축 + severity)를 초기 슬라이더 값으로 매핑하는 작업이 새로 필요하다 — 이전 화면의 단순 복원이 아니다.

### 5.5. [View 5] T-Shift / 효과검증 모듈 (미래 기능임을 명확히 표시) — **미구현: 코드에 존재하지 않음**
"정책은 실험하고 효과는 증명합니다" 컨셉은 유지하되, 현재 실제 정책 성과처럼 보이지 않도록 **"정책 시행 후 효과검증 프레임"**으로 명시한다.
```
정책 시행 → 비교지역 선정 → 사전/사후 데이터 수집 → DiD 등으로 실제 정책효과 검증
```

---

## 6. UI 표현 규칙
* "실시간 방문자" 등 초/분 단위를 암시하는 표현 금지 (3.4 참조)
* 종합점수(예: "○○시 68점")를 헤드라인으로 쓰지 않는다. 점수 위/아래에는 반드시 "어느 부분이 부족한가 / 왜 부족한가 / 그래서 무엇을 해야 하는가"를 문장으로 먼저 제시한다.
* 실측/파생/모델추정/향후분석가능 4단계 라벨을 모든 수치 옆에 유지한다 (3.3 참조).
* 문자열 내 `\n` 같은 escape 문자가 화면에 그대로 노출되는 버그는 발견 즉시 수정한다. JSX에서 개행이 필요하면 `\n`이 아니라 `<br />` 또는 별도 문자열/줄바꿈 요소를 사용한다.

---

## 7. 개발 우선순위 (현재)

**새로운 UI 기능을 늘리지 않는다.** 아래 4개 로직 완성이 최우선이다.
1. **[완료]** 유사지역 비교 (Peer Group Comparison) — 행정유형·수도권 여부·관광수요/인구/인구밀도 유사도 기반, 전국 비교와 분리
2. **[완료]** 취약영역 탐지 (Weakness Detection) — Peer Group 하위 25% 분위보다 그 그룹 IQR의 0.5배 이상 낮을 때만 취약 판정(고정 상수 아님, 미달 폭 반영). 근소 미달은 "근소"로만 표시하고 정책 과제에서 제외
3. **[완료]** 원인분해 (Root-Cause Decomposition) — 숙박비중 취약 시 관광사업자 원자료 자동 연결
4. **[완료]** 정책 우선순위 (Policy Priority Ranking) — severity 기반 TOP 3

이후 순서:
5. **[1차 완료, 전체 완료는 아님]** 전국 시군구 전수진단 1차 실행(2026-09-01, 상위 수요 100개 지역) 및 대표 사례 3개 선정 — 결과는 [`data/national_diagnosis_findings.md`](../data/national_diagnosis_findings.md) 참고. 하위 수요 구간(수요부족형·잠재력형 후보)은 아직 스캔하지 않았고, 대표 사례 3곳(평택·양평·하남)은 MOIS 원자료 자동연결 4개 지역 밖이라 원인분해 근거를 직접 못 붙였다 — 남은 작업은 위 문서의 "한계" 절 참고.
6. **[완료]** 기존 방문객 중심 분석과 결론이 달라지는 사례 문서화 (공모전 핵심 근거) — `data/national_diagnosis_findings.md`의 대표 사례 3개
7. **[보류]** 예산 시뮬레이터 재도입 — §5.4 참고. 화면에 없는 상태이며, 재도입은 별도 작업으로 취급한다.
8. UI 문자열 버그(`\n` 노출 등) 정리

---

## 8. 개발 및 검증 마일스톤
1. **데이터 인프라 구축**: 한국관광 데이터랩 지표 수집 및 DB 스키마 설계
2. **원인진단 파이프라인 구현**: 유사지역 비교 → 취약영역 탐지 → 원인분해 → 정책 우선순위 (§4)
3. **UI 재구성**: §5의 Flow 순서로 화면 재배치, 점수 후면화, 진단 문장 전면화
4. **전수진단 & 사례 발굴**: 전국 시군구 진단 후 대표 3개 지역 선정, 정책판단이 달라지는 사례 정리
5. **실무자 검증 및 고도화**: 지자체 관광 실무자 피드백 수렴, 데이터 메타 경고문 보완
