# Regional Tourism Scan 데이터 발급·연결 목록

이 문서는 전국 비교, 구조 기반 Peer Group, 원인진단을 위해 필요한 원천을 정리한다. API 키는 서버 환경변수에만 저장하며, 브라우저 코드에는 넣지 않는다.

## 1. 현재 키로 바로 사용할 수 있는 데이터

`KTO_TOURISM_DATALAB_API_KEY` 하나로 이미 승인받은 아래 한국관광공사 서비스를 사용한다. 별도 키 발급은 필요 없다.

| 데이터 | 쓰임 | 상태 |
|---|---|---|
| 빅데이터 지역별 방문자수_GW | 전국 관광수요, 외지인 방문자 백분위 | 연결됨 |
| 지역별 관광 수요 강도 | 체류강도, 숙박비중, 소비강도 | 연결됨 |
| 지역별 관광 다양성 | 보조 진단 지표 | 연결됨 |
| 관광지 집중률 방문자 추이 예측 | 혼잡 위험 보조 진단 | 연결됨 |
| 기초지자체 중심 관광지 정보 | 중심 관광지 좌표 기반 공간확산 보조지표 | 연결됨 |
| 지역별 관광 자원 수요 | SNS·카드·내비 수요 신호로 원인진단 보강 | 승인됨, **상세기능명 필요** |
| 행정안전부_문화_관광사업자 조회서비스 | 관광숙박업 등 영업 중 관광사업체 수 | 승인됨, **호출 URL·응답 필드 필요** |

### 지역별 관광 자원 수요에서 필요한 것

공공데이터포털의 활용신청 상세 화면에서 Swagger의 `GET /...` 경로 마지막 부분(오퍼레이션명)과 요청·응답 필드 화면을 보내면 된다. 새 키가 아니라 기존 KTO 키를 쓴다. 특히 아래 신호가 있는 상세기능을 우선 연결한다.

- 내비게이션 목적지 검색량 또는 이동/연계 지표
- SNS 언급량·여행유형별 수요
- 신용카드 기반 관광자원 소비 신호

## 2. 반드시 추가 신청할 데이터

### A. 숙박 공급 원인진단

숙박 성과가 낮을 때 `공급 부족`과 `체류 콘텐츠 부족`을 구분하는 핵심 원천이다.

`행정안전부_문화_관광사업자 조회서비스` 승인이 완료됐으므로, 우선 관광숙박업의 **영업 중 업소 수**를 숙박 공급 변수로 사용한다. 활용신청 상세 화면의 호출 URL과 응답 필드 화면을 보내면 바로 연결한다.

객실 수까지 필요한 고도화 단계에서는 공공데이터포털에서 아래 서비스를 추가 활용신청한다.

1. **행정안전부_문화_숙박업 조회서비스**
2. **행정안전부_문화_관광숙박업 조회서비스**

필요 필드:

- 영업상태, 인허가일·폐업일
- 시도·시군구 또는 주소
- 업소 유형
- 객실 수(제공되는 경우)

두 서비스는 전국 인허가 사업체 원천이다. 객실 수가 누락된 사업체가 있을 수 있으므로, 결과 화면에는 `업소 수`와 `확인 가능한 객실 수`를 구분해 표기한다. 동일한 Data.go.kr 키가 발급되어 있어도 서비스별 활용신청은 별도로 필요할 수 있다.

### B. 관광지 간 이동·연계 원인진단

공공데이터포털에서 아래 서비스를 활용신청한다.

3. **한국관광공사_관광지별 연관 관광지 정보**

용도:

- 관광지-관광지, 관광지-음식, 관광지-숙박 연계의 약함을 확인
- `관광지 간 이동 부족` 원인과 `관광동선 연결` 정책의 근거 제공

이 서비스도 KTO 공통 키를 쓰며, 승인 후 상세기능의 오퍼레이션명과 요청·응답 필드를 전달받아 연결한다.

## 3. 이미 받은 KOSIS 키로 설정할 항목

새 KOSIS 키 발급은 필요 없다. 현재 키에 다음 통계표를 선택해 **통계표 ID와 생성된 API URL의 분류조건**을 확보해야 한다.

| 구조 변수 | 권장 KOSIS 통계표 | 용도 |
|---|---|---|
| 시군구 총인구 | `DT_1B040A3` 행정구역(시군구)별 성별 인구수 | 인구 규모 |
| 시군구 인구밀도 | 인구총조사 시군구 인구밀도 표 또는 면적 표 + 총인구 | 밀도·도시성 |
| 시군구 면적 | 지자체 기본통계 또는 지적통계의 행정구역별 면적 표 | 인구밀도 계산·면적 통제 |
| 도시/농촌 성격 | 인구밀도와 행정구역 유형으로 파생 | Peer Group 구조 변수 |

KOSIS 기본 경로는 `https://kosis.kr/openapi/Param/statisticsParameterData.do?method=getList`로 설정한다. 키만으로 수치를 바로 조회할 수 없고, 여기에 `orgId`, `tblId`, 분류값(`objL*`), 항목, 시점 조건이 포함되어야 한다. 통계표 선택 화면에서 시군구 전체·최근 연도·JSON으로 설정한 뒤, 생성 URL에서 `apiKey`를 제외한 나머지 쿼리 문자열을 보내면 서버 설정으로 옮긴다.

## 4. 선택적으로 추가할 데이터

| 데이터 | 필요 시점 | 용도 |
|---|---|---|
| 철도역·공항 위치/운행 데이터 | Peer v2 | 광역권 접근성 점수 |
| 지방재정365 관광·문화 세출 | 고도화 단계 | 정책 여력·사후검증 보조 변수 |
| 관광지별 실제 방문자 수 또는 점유율 | TCEI 정식 D 단계 | 방문점유율 HHI 기반 공간분산 |

현재 승인된 중심 관광지 API는 좌표·연계 중심성 데이터이므로, 이를 이용한 공간확산 거리는 보조지표로만 쓴다. 관광지별 실제 방문점유율이 확보될 때에만 AGENTS.md의 정식 공간분산 D를 산출한다.

## 5. 데이터베이스 연결 정보

전국 월별 적재와 Peer Group을 운영하려면 배포 환경에서 접근 가능한 PostgreSQL이 필요하다. 아래 중 하나를 준비한다.

- Neon 또는 Supabase PostgreSQL 프로젝트
- 기존 PostgreSQL 서버

발급 후 `DATABASE_URL`만 Vercel Production/Preview 환경변수와 로컬 `.env`에 설정한다. 예: `postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require`

## 6. 발급·전달 우선순위

1. 관광사업자 조회서비스의 호출 URL·응답 필드 화면 전달
2. 한국관광공사 관광지별 연관 관광지 정보 활용신청
3. KOSIS 시군구 총인구·면적/인구밀도 생성 URL 확보
4. 지역별 관광 자원 수요의 오퍼레이션명과 Swagger 화면 확보
5. 객실 수까지 고도화하려면 숙박업·관광숙박업 조회서비스 활용신청
6. PostgreSQL 연결 문자열 등록
